# Money Tracker

A basic expense tracker app built with React. It intentionally has a bug, poor UI, and messy code — all of which I'm learning to fix.

## Getting Started

```bash
npm install
npm run dev
```

Then open your browser at `http://localhost:3000`.
